<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <!-- logo setting -->
      <img class="navbar-brand" src="/Assets/images/logo.png" href="/">Min Li</a>

      <!-- navbar setting -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <!-- turn the buttoms to bright when click -->
          <li class="nav-item">
          <% if (title == "Home") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
             href="/"><i class="fas fa-lg fa-dungeon"></i>Home</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "About") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
             href="/about"><i class="fab fa-lg fa-stack-exchange"></i>About</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "Projects") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/projects"><i class="fab fa-lg fa-app-store-ios"></i>Projects</a>
          </li>
  
          <li class="nav-item">
          <% if (title == "Services") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/services"><i class="fas fa-lg fa-hands-helping"></i>>Services</a>
          </li>
          
          <li class="nav-item">
          <% if (title == "Contact Me") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/contact"><i class="fas fa-lg fa-user-friends"></i>Contact Me</a>
          </li>

          <!-- Contacts List Secure area and Authentication Links -->
          <li class="nav-item">
          <% if (title == "Contacts") { %>
            <a class="nav-link active"
          <% } else { %>
            <a class="nav-link"
          <% } %>
            href="/contacts-list"><i class="fas fa-lg fa-qrcode"></i>Contacts</a>
          </li>       

          <% if(displayName) { %>
            <li class="nav-item">
              <% if (title != "Login") { %>
                <a class="nav-link active"
              <%} else {%>
                <a class="nav-link"
              <% } %>
              href="/logout"><i class="fas fa-sign-out-alt fa-lg"></i> Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <% if (title == "Login") { %>
                <a class="nav-link active"
              <%} else {%>
                <a class="nav-link"
              <% } %>
              href="/login"><i class="fas fa-sign-in-alt fa-lg"></i> Login</a>
            </li>
          <% } %>
  
      </div>
    </div>
  </nav>